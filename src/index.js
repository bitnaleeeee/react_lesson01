import React, { useState } from 'react';
import ReactDOM from 'react-dom/client';
import Router from './Router.js';
import Resset from './styles/Reset.scss';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<Router />);
